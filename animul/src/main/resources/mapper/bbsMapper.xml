<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="bbs">
		<insert id="insert" parameterType="bbsVO" >
		insert into bbs values (
		null,
		#{member_id}
		#{bbs_title},
		#{bbs_content},
		#{bbs_writer},
		now(),
		#{bbs_hit}
		)
	</insert>
	<update id="update" parameterType="bbsVO" >
		update bbs set bbs_title = #{bbs_title}, bbs_content = #{bbs_content} where member_id = #{member_id}
	</update>
	<delete id="delete" parameterType="bbsVO" >
		delete from bbs where member_id = #{member_id}
	</delete>
	<select id="one" parameterType="bbsVO" resultType="bbsVO">
		select * from bbs where member_id = #{member_id}
	</select>
	<select id="list" resultType="bbsVO">
		select * from bbs order by bbs_id desc
	</select>
	
</mapper>